class Room:
        def __init__(self, roomid, userid, maxp, refcode, roomname):
            self.roomid = roomid
            self.userid = userid
            self.maxp = maxp
            self.refcode = refcode
            self.roomname = roomname
